apiVersion: v1
kind: Service
metadata:
  name: activemq-dashboard-backend
  namespace: activemq-dashboard
spec:
  type: ClusterIP
  selector:
    app: activemq-dashboard-backend
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
